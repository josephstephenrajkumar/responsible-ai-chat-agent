def evaluate_controllability(message):
    return {
        'controllability_properties': ['temperature', 'max_tokens', 'mode'],
        'recommendation': 'Use explicit parameters to guide generation behavior'
    }
